node{
    stage ('Build') 
    {
        sh 'git clone https://github.com/kubraokumus1/gradle-test'
        sh 'cd gradle-test'
        sh 'ls -la'
        sh 'pwd'
        sh './gradlew build'
    }
}
